import styled from "styled-components";

export const Aside = styled.aside`
  background-color: var(--card-color);
  height: fit-content;
  padding: 1rem;
  border-radius: 10px;
  position: sticky;
  top: 11%;
`;
